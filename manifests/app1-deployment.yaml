# Déploiement de l'application 1
# Crée et maintient 1 pod avec un serveur web simple

apiVersion: apps/v1
kind: Deployment
metadata:
  name: app1
  namespace: applications-web
  labels:
    app: app1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app1
  template:
    metadata:
      labels:
        app: app1
    spec:
      containers:
      - name: app1
        image: paulbouwer/hello-kubernetes:1.10
        ports:
        - containerPort: 8080
        env:
        - name: MESSAGE
          value: Hello from App 1
        - name: KUBERNETES_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace

# Source Image : https://hub.docker.com/r/paulbouwer/hello-kubernetes/